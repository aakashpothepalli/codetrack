 let editorDefault = `#include<bits/stdc++.h>
#define ll long long
#define ull unsigned long long
#define  in(ar,n) for(int i=0;i<n;i++)cin>>ar[i];
#define out(ar,n) \
    for(int i=0;i<n;i++) \
    cout<<ar[i]<<" "; \
    cout<<endl;
#define for2d(n,m) for(int i=0;i<n;i++)for(int j=0;j<m;j++)
#define fori(i,a,b) for(ll i=a;i<b;++i)
#define all(ar) ar.begin(),ar.end()
#define fastio ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
using namespace std;
typedef pair<ll,ll> pll;
typedef pair<int,int> pii;
typedef vector<vector<int>> vvi;
const ll mod = 1e9 + 7;

/**
    keywords for autocomplete
    push_back() insert() pop_back() front() top() back()
    sort reverse
    unordered_map map vector set priority_queue stack queue
*/

int main(){
    int t;cin>>t;
    while(t--){
        int n;cin>>n;
        ll ar[n];
        in(ar,n);
    }
}

`;
export default editorDefault;